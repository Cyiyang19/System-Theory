# 交付與驗收紀錄

## 已完成的範圍

- CH1–5 先備知識導讀、CH6 四框架完整講義與 CH7 銜接。
- 30／45／60 分鐘課程安排與講者稿。
- 兩套由官方 existing codes 改編的免 API key 實驗。
- 小組實驗單、學生待完成程式、教師答案、繳交模板。
- 原始碼副本、固定上游 commit、修改 diff、來源與授權。
- 固定套件版本、Windows setup／demo 入口。

## 執行驗收

實際紀錄在 outputs/environment_report.json 與 outputs/pytest.txt：

- 16 項測試通過。
- LangGraph baseline、重試、短暫故障、持續故障、未知問題、零重試、過高門檻均符合預期。
- AutoGen 修正後 QA_PASS；持續不修正則到上限並回報 needs_help。
- 另建立全新隔離環境，只安裝 requirements.txt，再重跑完整驗證通過；以該乾淨環境產生 requirements-lock.txt。
- setup.ps1 實際執行到 SETUP PASS；demo.cmd 實際啟動並操作 1、2、4、5 後正常離開。
- Markdown 相對連結已檢查存在。

故意留下的 student_policy.py 是練習起點，檢查器在修改前應失敗。它不是一個被掩蓋的已完成解答。

## 公開 repository 的範圍

放在 Cyiyang19/System-Theory 的 hello-agents-ch6-lab 資料夾。部署的意思是上傳可下載與閱讀的講義、程式和驗證資料；**沒有建立線上 LLM 服務**。GitHub Actions 配置位於 repository 根目錄的 .github/workflows/ch6-lab.yml，會在推送後重新安裝並驗證。

## 尚未做的項目

- 依最新指示，不製作影片，改用課堂現場 demo；若老師的錄影要求仍有效，課堂錄影仍需補交。
- 不測試線上 LLM、Tavily、AgentScope 或 CAMEL runtime。
- 不把 AI 協助整理的內容當成本人已理解；上台前請照「最簡單教會你」親自走一次。
